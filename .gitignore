# Terraform provider binaries & cache — regenerated by 'terraform init'
.terraform/

# State — never commit (holds secrets; lives in S3)
*.tfstate
*.tfstate.*

# Plans, crash logs, secret vars
*.tfplan
crash.*.log
*.tfvars
